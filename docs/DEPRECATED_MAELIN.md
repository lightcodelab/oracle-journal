# Maelin / HealingBot — deprecated 2026-07-20

Superseded by the AreekeerA® Protocol Builder at `/devotion/areekeera`.

Retired:
- Route `/devotion/healing-bot` now redirects to `/devotion/areekeera`.
- `DoorOfDevotion` no longer surfaces the Maelin entry.

Preserved for retention review (do not delete without approval):
- `src/pages/HealingBot.tsx` (unmounted).
- `supabase/functions/healing-bot/` (still deployed; can be turned off after review).
- `public.healing_conversations` table (member data — retention policy pending).

Do not describe the AreekeerA Protocol Builder as a generic chatbot. It is a
structured, symptom-informed, safety-aware protocol-building experience.